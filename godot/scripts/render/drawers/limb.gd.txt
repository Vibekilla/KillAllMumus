## AUTO-PORTED from HTML limb — hand-fixed where needed
func limb(x1, y1, x2, y2, w, col) -> void:
	 ctx.stroke_style=col; ctx.line_width=w; ctx.begin_path(); ctx.move_to(x1,y1); ctx.line_to(x2,y2); ctx.stroke(); 
