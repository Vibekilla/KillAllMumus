# AUTO-PORTED from public/index.html — needs manual polish
# Original: circle
extends RefCounted
# expects `ctx` CanvasCompat and game globals

func circle(x,y,r,fill,line) -> void:
 ctx.begin_path(); ctx.arc(x,y,r,0,7); if (line): # {ctx.stroke_style(line);ctx.line_width(2);ctx.stroke();# } ctx.fill_style(fill); ctx.fill(); 