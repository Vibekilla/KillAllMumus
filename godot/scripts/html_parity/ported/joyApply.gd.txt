## AUTO-PORTED from HTML function joyApply — review required
func joyApply(t):
	 var dx=t.clientX-joy.cx, dy=t.clientY-joy.cy; var d=sqrt_sum_sq(dx,dy); if(d>JOY_R){ dx*=JOY_R/d; dy*=JOY_R/d; }
	  joy.vx=dx/JOY_R; joy.vy=dy/JOY_R; joyknob.style.transform="translate(calc(-50% + " + str(dx) + "px), calc(-50% + " + str(dy) + "px))"; 
