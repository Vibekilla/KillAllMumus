## AUTO-PORTED from HTML function cycleSpecial — review required
func cycleSpecial():
	 if(!run||!run.specials||run.specials.size()<2)return; run.armed=(run.armed+1)%run.specials.size(); var sp=armedSpec(); sfx('item'); flashMsg={t:60,txt:'SPECIAL ▸ '+(sp?sp.name:'—')}; 
