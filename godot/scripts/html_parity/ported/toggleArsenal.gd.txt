## AUTO-PORTED from HTML function toggleArsenal — review required
func toggleArsenal(type, key):

	  var arr=arsArr(type), cap=ARS_CAP[type], minKeep=(type=='s'||type=='i'?0:1);   // weapons & melee need ≥1; specials & items optional
	  var i=arr.find(key);
	  if(i>=0){ if(arr.size()>minKeep){ arr.splice(i,1); sfx('item'); } else { sfx('hit'); return; } }
	  else { if(arr.size()<cap){ arr.append(key); sfx('power'); } else { sfx('hit'); return; } }
	  saveArsenal(); if(run) applyArsenalToRun();

