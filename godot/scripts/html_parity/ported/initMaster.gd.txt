## AUTO-PORTED from HTML function initMaster — review required
func initMaster():
	 var ac=actx(); if(ac&&!masterGain){ masterGain=ac.createGain(); masterGain.gain.value=sfxVol; masterGain.connect(ac.destination); } 
