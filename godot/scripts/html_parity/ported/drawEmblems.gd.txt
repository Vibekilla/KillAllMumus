## AUTO-PORTED from HTML function drawEmblems — review required
func drawEmblems():

	  var g=ctx.createLinearGradient(0,0,0,H); g.addColorStop(0,'#1a0e26'); g.addColorStop(1,'#2a1020'); ctx.fillStyle=g; ctx.fillRect(0,0,W,H);
	  ctx.textAlign='center';
	  ctx.save(); ctx.shadowColor='#ffd27a'; ctx.shadowBlur=18; ctx.fillStyle='#ffe08a'; ctx.font='900 34px "Trebuchet MS"'; ctx.fillText('🏅 EMBLEMS', W/2, 44); ctx.restore();
	  ctx.fillStyle='#c8b0d0'; ctx.font='12px monospace'; ctx.fillText('Achievements · '+emblemCount()+' / '+EMBLEMS.size()+' unlocked · some grant skins', W/2, 64);
	  var pbw=460, pbx=W/2-pbw/2; ctx.fillStyle='#2a1a30'; ctx.beginPath(); ctx.roundRect(pbx,72,pbw,7,3); ctx.fill();
	  ctx.fillStyle='#ffd27a'; ctx.beginPath(); ctx.roundRect(pbx,72,pbw*emblemCount()/EMBLEMS.size(),7,3); ctx.fill();
	  if(emPage>=emPageCount()) emPage=emPageCount()-1;
	  // paginated grid — 4×3 roomy cards per page
	  var cols=4, rows=3, gap=8, gy=90, botY=H-56, gx=24;
	  var cw=(W-2*gx)/cols, ch=minf(92,(botY-gy-(rows-1)*gap)/rows);
	  var start=emPage*EM_PER_PAGE, pageItems=EMBLEMS.slice(start, start+EM_PER_PAGE);
	  for i in range(int(0), int(pageItems.size())){ var e=pageItems[i], got=hasEmblem(e.id), secret=e.secret&&!got;
	    var cx=gx+(i%cols)*cw, cy=gy+floorf(i/cols)*(ch+gap);
	    ctx.fillStyle=got?'rgba(255,210,120,0.12)':'rgba(255,255,255,0.03)'; ctx.beginPath(); ctx.roundRect(cx+3,cy,cw-6,ch,10); ctx.fill();
	    ctx.strokeStyle=got?'rgba(255,210,120,0.55)':'rgba(255,255,255,0.08)'; ctx.lineWidth=1.3; ctx.stroke();
	    ctx.textAlign='center'; ctx.globalAlpha=got?1:0.35; ctx.font='26px serif'; ctx.fillText(got?(e.icon||'🏅'):'🔒', cx+27, cy+30); ctx.globalAlpha=1;
	    ctx.fillStyle=got?'#ffe08a':'#8a7a92'; ctx.font='bold 12px "Trebuchet MS"'; ctx.textAlign='left'; ctx.fillText((secret?'???':e.name).slice(0,20), cx+46, cy+20);
	    ctx.fillStyle=got?'#d0c0da':'#6a5a72'; ctx.font='9px monospace'; wrapText(secret?'Hidden — keep playing.':e.desc, cx+46, cy+34, cw-54, 10);
	    if(e.outfit && !secret){ var sn=(OUTFITS.find(func(o): return o.key==e.outfit)||{}).name||e.outfit; ctx.fillStyle=got?'#8fd0a0':'#6a7a6a'; ctx.font='bold 8.5px monospace'; ctx.fillText((got?'👗 ':'👗 ')+sn, cx+46, cy+ch-8); }
	  }
	  // pager
	  var pc=emPageCount();
	  if(pc>1){ var ny=H-46, bw=94, bh=28; emPrevBtn={x:W/2-150,y:ny,w:bw,h:bh}; emNextBtn={x:W/2+56,y:ny,w:bw,h:bh};
	    var navBtn=func(b,label,on): # arrow
	 ctx.fillStyle=on?'rgba(40,20,50,0.9)':'rgba(30,18,38,0.4)'; ctx.beginPath(); ctx.roundRect(b.x,b.y,b.w,b.h,7); ctx.fill(); ctx.strokeStyle=on?'#ff8ac0':'rgba(255,140,200,0.25)'; ctx.lineWidth=1.5; ctx.stroke(); ctx.fillStyle=on?'#ffd6ea':'#6a5a72'; ctx.font='bold 13px monospace'; ctx.textAlign='center'; ctx.fillText(label, b.x+b.w/2, b.y+18); ctx.textAlign='left'; };
	    navBtn(emPrevBtn,'◀ PREV', emPage>0); navBtn(emNextBtn,'NEXT ▶', emPage<pc-1);
	    ctx.textAlign='center'; ctx.fillStyle='#c8b0d0'; ctx.font='bold 13px monospace'; ctx.fillText('Page '+(emPage+1)+' / '+pc, W/2, H-28); ctx.textAlign='left';
	  } else { emPrevBtn=emNextBtn=null; }
	  ctx.textAlign='center'; ctx.fillStyle=(floorf(tick/30)%2)?'#fff':'#9a7c96'; ctx.font='bold 13px monospace'; ctx.fillText('PRESS '+kb('shoot')+' / TAP EMPTY AREA TO RETURN', W/2, H-8); ctx.textAlign='left';
	}
	//  -= 1 -= 1 OUTFITS gallery: pick a skin + watch her do cute animations in a preview stage  -= 1 -= 1
	var outfitPreview='og', outfitPose=func((): # arrow
	 try{ return int(HtmlBridge.storage_get('bobina_pose'))||0; }catch(e){ return 0; } })(), victoryFace=func((): # arrow
	 try{ return int(HtmlBridge.storage_get('bobina_face'))||0; }catch(e){ return 0; } })(), outfitAnimT=0, outfitTiles=[], outfitPoseBtn=null, outfitBackBtn=null, faceBtn=null;   // selected pose + face persist — her stage-clear victory pose
	var OUTFIT_POSES=[{name:'· Idle'},{name:'♪ Dance'},{name:'✦ Twirl'},{name:'♥ Bounce'},{name:'>v< Cheer'},{name:'🔥 This Is Fine'}];   // the SELECTED pose becomes her stage-clear victory pose (extensible — add more here + a poseParams case)
	var VICTORY_FACES=[{name:'Auto', expr:null},{name:':3', expr:'uwu'},{name:'Smile', expr:'smile'},{name:'>v<', expr:'squee'},{name:'Giggle', expr:'giggle'},{name:'Annoyed', expr:'annoyed'}];   // face the player can pick for the victory pose (Auto = the pose's own face)
	// Motion + expression for each pose. Add a case here (and an entry to OUTFIT_POSES) and it works EVERYWHERE —
	// the outfit preview AND her on-field stage-clear victory pose (which is whichever pose the player selected).

