## AUTO-PORTED from HTML function doSaveScore — review required
func doSaveScore():
	 submitScore(NEinput.value); hideNameEntry(); justSavedScore=true; state = endWon ? 'win' : 'gameover'; }  // return to the result screen so they can still share
	HtmlBridge.doc.getElementById('ne-save').onclick=doSaveScore;
	HtmlBridge.doc.getElementById('ne-skip').onclick=func(): # arrow
	 hideNameEntry(); };
	NEinput.HtmlBridge.add_listener('keydown',func(e): return { e.stopPropagation(); if(e.key=='Enter'){ doSaveScore(); } });

