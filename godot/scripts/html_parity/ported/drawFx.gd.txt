## AUTO-PORTED from HTML function drawFx — review required
func drawFx():

	  for f in fx){
	    if(f.type=='laser'){ var px=isFinite(f.x)?f.x:(player?player.x:PF.x+PF.w/2), py=isFinite(f.y)?f.y:(player?player.y:PF.y+PF.h-70), ang=isFinite(f.ang)?f.ang:-PI/2;
	      ctx.save(); ctx.beginPath(); ctx.rect(PF.x,PF.y,PF.w,PF.h); ctx.clip();
	      ctx.translate(px,py); ctx.rotate(ang+PI/2); var L=PF.w+PF.h, hw=f.w/2;
	      ctx.globalAlpha=0.85*minf(1,(64-f.t)/6)*minf(1,f.t/12);
	      var g=ctx.createLinearGradient(-hw,0,hw,0); g.addColorStop(0,'rgba(168,85,247,0)'); g.addColorStop(0.5,'#a855f7'); g.addColorStop(1,'rgba(168,85,247,0)');
	      ctx.fillStyle=g; ctx.fillRect(-hw,-L,f.w,L); ctx.fillStyle='rgba(236,220,255,0.95)'; ctx.fillRect(-4,-L,8,L); ctx.restore(); }
	    else if(f.type=='mech'){ var fade=f.t<40? f.t/40 : (f.t>210?(240-f.t)/30:1);
	      drawMech(f.x,f.y, fade, ((f.face!=null?f.face:-PI/2)+PI/2));   // turns to follow her orientation
	      if(player && !player.dead){ var _sc=bodyCtr(player); ctx.save(); ctx.globalCompositeOperation='lighter'; ctx.translate(_sc.x,_sc.y); ctx.globalAlpha=0.6*fade;   // shield bubble
	        ctx.strokeStyle='#8fb8ff'; ctx.lineWidth=2.4; ctx.shadowColor='#8fb8ff'; ctx.shadowBlur=12; ctx.beginPath(); ctx.arc(0,0,27,0,7); ctx.stroke();
	        ctx.strokeStyle='rgba(220,235,255,0.7)'; ctx.lineWidth=1; for i in range(int(0), int(5)){ var a=tick*0.06+i*1.257; ctx.beginPath(); ctx.arc(0,0,27,a,a+0.4); ctx.stroke(); } ctx.restore(); ctx.globalAlpha=1; } }
	    else if(f.type=='bearzooka'){ drawMech(f.x,f.y, minf(1,f.t/20)*minf(1,(156-f.t)/12), PI*0.5); }   // SKOL gunship strafing across the top
	    else if(f.type=='bombdrop'){ ctx.save(); ctx.translate(f.x,f.y);
	      ctx.fillStyle='rgba(255,170,60,0.75)'; ctx.beginPath(); ctx.moveTo(-3.5,7); ctx.lineTo(0,16+sin(tick*0.5)*3); ctx.lineTo(3.5,7); ctx.closePath(); ctx.fill();   // fiery trail
	      ctx.fillStyle='#33333c'; ctx.beginPath(); ctx.ellipse(0,0,5,8.5,0,0,7); ctx.fill();   // bigger bomb body
	      ctx.fillStyle='#8a8a95'; ctx.beginPath(); ctx.ellipse(-1.6,-2,1.6,3,0,0,7); ctx.fill();   // shine
	      ctx.fillStyle='#ffb04a'; ctx.beginPath(); ctx.moveTo(-4.5,-6.5); ctx.lineTo(0,-13); ctx.lineTo(4.5,-6.5); ctx.closePath(); ctx.fill();   // fins
	      ctx.fillStyle=(tick%4<2)?'#fff':'#ff6a2a'; ctx.beginPath(); ctx.arc(0,-13,1.4,0,7); ctx.fill(); ctx.restore(); }   // fuse spark
	    else if(f.type=='blackhole'){ var t=tick, R=15+(f.r||0);
	      ctx.save(); ctx.globalCompositeOperation='lighter'; ctx.translate(f.x,f.y);
	      var g=ctx.createRadialGradient(0,0,4,0,0,64); g.addColorStop(0,'rgba(60,230,120,0.42)'); g.addColorStop(0.4,'rgba(30,160,70,0.2)'); g.addColorStop(1,'rgba(0,0,0,0)'); ctx.fillStyle=g; ctx.beginPath(); ctx.arc(0,0,64,0,7); ctx.fill();
	      for k in range(int(0), int(5)){ var rr=8+k*7, a0=t*0.16*(k%2?1:-1)+k*1.3; ctx.strokeStyle="rgba(90,235,140," + str(0.55-k*0.08) + ")"; ctx.lineWidth=2.6-k*0.35; ctx.beginPath(); ctx.arc(0,0,rr,a0,a0+3.6); ctx.stroke(); }   // swirl
	      ctx.restore();
	      ctx.save(); ctx.translate(f.x,f.y); ctx.fillStyle='#04160b'; ctx.beginPath(); ctx.arc(0,0,f.r||8,0,7); ctx.fill(); ctx.strokeStyle='#3ae66a'; ctx.lineWidth=1.6; ctx.stroke(); ctx.restore(); }   // dark core
	    else if(f.type=='wave'){ if(f.delay>0)continue; ctx.save(); ctx.globalAlpha=maxf(0,1-f.r/(PF.w)); ctx.strokeStyle='#ffd27a'; ctx.lineWidth=5; ctx.shadowColor='#ffd27a'; ctx.shadowBlur=14; ctx.beginPath(); ctx.arc(f.x,f.y,f.r,0,7); ctx.stroke(); ctx.strokeStyle='#fff'; ctx.lineWidth=2; ctx.beginPath(); ctx.arc(f.x,f.y,f.r,0,7); ctx.stroke(); ctx.restore(); }
	    else if(f.type=='bull'){ ctx.save(); ctx.translate(f.x,f.y); ctx.globalAlpha=minf(1,f.t/12);
	      ctx.fillStyle='rgba(191,230,160,0.5)'; for i in range(int(0), int(3)){ ctx.beginPath(); ctx.arc(-7+i*7,14+((f.t*3+i*9)%10),2.4,0,7); ctx.fill(); }  // dust
	      ctx.save(); ctx.shadowColor='#7ed957'; ctx.shadowBlur=14; ctx.fillStyle='#7ed957';
	      ctx.beginPath(); ctx.ellipse(0,0,15,11,0,0,7); ctx.fill(); ctx.beginPath(); ctx.ellipse(0,-11,8.5,7.5,0,0,7); ctx.fill();
	      ctx.fillStyle='#d6ffa8'; ctx.beginPath(); ctx.moveTo(-6,-16); ctx.lineTo(-11,-23); ctx.lineTo(-2,-17); ctx.closePath(); ctx.fill(); ctx.beginPath(); ctx.moveTo(6,-16); ctx.lineTo(11,-23); ctx.lineTo(2,-17); ctx.closePath(); ctx.fill();
	      ctx.fillStyle='#c0202a'; circle(-3,-11,1.6,'#c0202a'); circle(3,-11,1.6,'#c0202a'); ctx.restore(); ctx.restore(); }
	    else if(f.type=='badger'){ ctx.save(); ctx.translate(f.x,f.y); ctx.scale(f.dir,1); ctx.globalAlpha=minf(1,f.t/12);
	      // honey dust trail
	      ctx.fillStyle='rgba(240,176,48,0.5)'; for i in range(int(0), int(4)){ ctx.beginPath(); ctx.arc(-18-i*6, 6+((f.t*3+i*7)%8), 2.6,0,7); ctx.fill(); }
	      ctx.save(); ctx.shadowColor='#f0b030'; ctx.shadowBlur=12;
	      // legs pumping + pale paws
	      ctx.fillStyle='#20201c'; ctx.beginPath(); ctx.ellipse(-7,10,3,4,0,0,7); ctx.ellipse(7,10,3,4,0,0,7); ctx.fill();
	      var lp=sin(f.t*0.7)*1.6; ctx.fillStyle='#c9b48a'; ctx.beginPath(); ctx.ellipse(-7+lp,13,2.2,1.5,0,0,7); ctx.ellipse(7-lp,13,2.2,1.5,0,0,7); ctx.fill();
	      // dark underside + pale badger mantle
	      ctx.fillStyle='#221f18'; ctx.beginPath(); ctx.ellipse(0,4,21,9,0,0,7); ctx.fill();
	      ctx.fillStyle='#dcd6c6'; ctx.beginPath(); ctx.ellipse(-2,-2,20,8,0,0,7); ctx.fill();
	      // head (dark)
	      ctx.fillStyle='#1c1a16'; ctx.beginPath(); ctx.ellipse(17,1,9.5,8,0,0,7); ctx.fill();
	      // signature white face stripe
	      ctx.fillStyle='#f4efe6'; ctx.beginPath(); ctx.moveTo(9,-6); ctx.lineTo(25,-6); ctx.lineTo(25,-2.5); ctx.lineTo(9,-2.5); ctx.closePath(); ctx.fill();
	      // rounded ear
	      ctx.fillStyle='#1c1a16'; ctx.beginPath(); ctx.arc(12,-7,2.6,0,7); ctx.fill();
	      // eye
	      ctx.fillStyle='#fff'; circle(19,-0.5,1.5,'#fff'); ctx.fillStyle='#100c08'; circle(19.6,-0.5,0.8,'#100c08');
	      // snout + nose
	      ctx.fillStyle='#efe8db'; ctx.beginPath(); ctx.ellipse(25,2,3.4,3,0,0,7); ctx.fill();
	      ctx.fillStyle='#100c08'; ctx.beginPath(); ctx.ellipse(27.4,2,1.6,1.3,0,0,7); ctx.fill();
	      // fearless snarl + fang
	      ctx.fillStyle='#7a1c22'; ctx.beginPath(); ctx.ellipse(24.5,5,2.2,1.4,0,0,7); ctx.fill();
	      ctx.fillStyle='#fff'; ctx.beginPath(); ctx.moveTo(23.4,4); ctx.lineTo(24.4,6.4); ctx.lineTo(25.4,4); ctx.closePath(); ctx.fill();
	      // front claws
	      ctx.strokeStyle='#eae2d0'; ctx.lineWidth=1.5; ctx.lineCap='round'; for i in range(int(0), int(3)){ ctx.beginPath(); ctx.moveTo(14,9+i*2.2); ctx.lineTo(21,8+i*2.2); ctx.stroke(); }
	      ctx.restore(); ctx.restore(); }
	    else if(f.type=='kiss'){ ctx.save(); ctx.globalAlpha=maxf(0,1-f.r/(PF.w*0.95)); ctx.strokeStyle='#ff5b8d'; ctx.lineWidth=5; ctx.shadowColor='#ff8ac0'; ctx.shadowBlur=16; ctx.beginPath(); ctx.arc(f.x,f.y,f.r,0,7); ctx.stroke();
	      ctx.shadowBlur=0; ctx.textAlign='center'; for i in range(int(0), int(12)){ var a=i/12*6.283+tick*0.02, hx=f.x+cos(a)*f.r, hy=f.y+sin(a)*f.r; ctx.font='13px serif'; ctx.fillText('💗',hx,hy+4); } ctx.textAlign='left'; ctx.restore(); }
	    else if(f.type=='bubble'){ ctx.save();
	      if(f.pop>0){ var a=maxf(0,f.pop/16); ctx.globalAlpha=a; ctx.strokeStyle='#bfe8ff'; ctx.lineWidth=3; ctx.shadowColor='#8fd0ff'; ctx.shadowBlur=12; ctx.beginPath(); ctx.arc(f.x,f.y,f.popR,0,7); ctx.stroke(); }
	      else { ctx.globalAlpha=0.62; var g=ctx.createRadialGradient(f.x-f.r*0.3,f.y-f.r*0.3,1,f.x,f.y,f.r); g.addColorStop(0,'rgba(255,255,255,0.5)'); g.addColorStop(0.6,'rgba(160,220,255,0.16)'); g.addColorStop(1,'rgba(120,190,255,0.34)'); ctx.fillStyle=g; ctx.beginPath(); ctx.arc(f.x,f.y,f.r,0,7); ctx.fill();
	        ctx.globalAlpha=0.9; ctx.strokeStyle='#cbeaff'; ctx.lineWidth=1.4; ctx.beginPath(); ctx.arc(f.x,f.y,f.r,0,7); ctx.stroke();
	        ctx.fillStyle='rgba(255,255,255,0.85)'; ctx.beginPath(); ctx.arc(f.x-f.r*0.35,f.y-f.r*0.4,f.r*0.16,0,7); ctx.fill(); }
	      ctx.restore(); }
	    else if(f.type=='stardust'){ ctx.save(); ctx.globalCompositeOperation='lighter';
	      for st in f.stars){ var fade=minf(1,st.t/4)*minf(1,(st.life-st.t)/7), tw=0.55+0.45*sin(tick*0.3+st.rot);
	        if(st.sapping){ ctx.strokeStyle="hsla(" + str(st.hue) + ",100%,74%," + str(0.45*fade) + ")"; ctx.lineWidth=1; ctx.beginPath(); ctx.moveTo(st.x,st.y); ctx.lineTo(st.sapX,st.sapY); ctx.stroke(); }   // sap beam to the Mumu
	        var s=st.sz*(0.7+0.5*tw); ctx.save(); ctx.translate(st.x,st.y); ctx.rotate(st.rot+tick*0.05);
	        ctx.fillStyle="hsla(" + str(st.hue) + ",100%,82%," + str(0.9*fade*tw) + ")"; ctx.shadowColor="hsl(" + str(st.hue) + ",100%,70%)"; ctx.shadowBlur=6;
	        ctx.beginPath(); ctx.moveTo(0,-s*2.4); ctx.lineTo(s*0.5,-s*0.5); ctx.lineTo(s*2.4,0); ctx.lineTo(s*0.5,s*0.5); ctx.lineTo(0,s*2.4); ctx.lineTo(-s*0.5,s*0.5); ctx.lineTo(-s*2.4,0); ctx.lineTo(-s*0.5,-s*0.5); ctx.closePath(); ctx.fill(); ctx.restore(); }
	      ctx.restore(); }
	    else if(f.type=='tentacle'){ var fade=minf(1,f.t/24)*minf(1,(360-f.t)/16); ctx.save(); ctx.translate(f.x,f.y); ctx.globalAlpha=fade;
	      ctx.save(); ctx.globalCompositeOperation='lighter'; ctx.globalAlpha=fade*0.35; ctx.fillStyle='#5fc9c9'; ctx.beginPath(); ctx.arc(0,0,f.reach*0.5,0,7); ctx.fill(); ctx.restore();
	      var segs=11; var pts=[]; for s in range(int(0), int(segs)){ pts.append([sin(f.ph+s*0.5)*s*1.5, -s*7.5]); }
	      ctx.lineCap='round'; ctx.lineJoin='round';
	      ctx.strokeStyle='#2f6d6d'; ctx.lineWidth=10; ctx.beginPath(); ctx.moveTo(pts[0][0],pts[0][1]); for q in pts) ctx.lineTo(q[0],q[1]); ctx.stroke();
	      ctx.strokeStyle='#5fc9c9'; ctx.lineWidth=5.5; ctx.beginPath(); ctx.moveTo(pts[0][0],pts[0][1]); for q in pts) ctx.lineTo(q[0],q[1]); ctx.stroke();
	      ctx.fillStyle='#bff0f0'; for(var s=2;s<=segs;s+=2){ ctx.beginPath(); ctx.arc(pts[s][0],pts[s][1],1.7,0,7); ctx.fill(); }
	      ctx.restore(); }
	    else if(f.type=='servitor'){ var t=tick, sz=f.sz||1, fade=minf(1,(600-f.t)/16)*minf(1,f.t/20); ctx.save(); ctx.translate(f.x,f.y);
	      ctx.save(); ctx.scale(sz,sz);
	      ctx.save(); ctx.globalCompositeOperation='lighter'; ctx.globalAlpha=fade*0.55; var g=ctx.createRadialGradient(0,0,1,0,0,18); g.addColorStop(0,'rgba(157,107,255,0.8)'); g.addColorStop(1,'rgba(0,0,0,0)'); ctx.fillStyle=g; ctx.beginPath(); ctx.arc(0,0,18,0,7); ctx.fill(); ctx.restore();
	      ctx.globalAlpha=fade;
	      ctx.strokeStyle='#3a1a5a'; ctx.lineWidth=2.2; ctx.lineCap='round'; for k in range(int(0), int(8)){ var a=t*0.05+k*0.785, ln=9+sin(t*0.11+k)*3.8; ctx.beginPath(); ctx.moveTo(0,0); ctx.lineTo(cos(a)*ln,sin(a)*ln); ctx.stroke(); }
	      ctx.fillStyle='#2a1040'; ctx.beginPath(); ctx.arc(0,0,7.5,0,7); ctx.fill();
	      ctx.fillStyle='#e0d0ff'; ctx.beginPath(); ctx.arc(0,0,4.4,0,7); ctx.fill();
	      ctx.fillStyle='#9d1bff'; ctx.beginPath(); ctx.arc(cos(t*0.1)*1.7,sin(t*0.1)*1.7,2.3,0,7); ctx.fill();
	      ctx.restore();
	      // health bar above its head
	      ctx.globalAlpha=fade; var bw=32,bh=5,bx=-bw/2,by=-12*sz-10, hf=maxf(0,minf(1,f.hp/(f.maxhp||f.hp)));
	      ctx.fillStyle='rgba(0,0,0,0.55)'; ctx.fillRect(bx-1,by-1,bw+2,bh+2); ctx.fillStyle='#241038'; ctx.fillRect(bx,by,bw,bh);
	      ctx.fillStyle=hf>0.5?'#b57aff':(hf>0.25?'#ffb04a':'#ff5b6e'); ctx.fillRect(bx,by,bw*hf,bh);
	      ctx.restore(); }
	  }

