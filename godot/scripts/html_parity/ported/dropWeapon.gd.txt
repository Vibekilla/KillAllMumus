## AUTO-PORTED from HTML function dropWeapon — review required
func dropWeapon(x, y):
	 dropItem(x,y,'fullpower'); }   // weapons now come only from the Arsenal loadout — the old weapon-drop slot rewards full power instead

