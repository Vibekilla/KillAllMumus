## AUTO-PORTED from HTML function killExtend — review required
func killExtend():
	 if(run.lives<MAX_LIVES){ run.lives += 1; sfx('extend'); flashMsg={t:110,txt:'MUMU SLAYER — 1UP!'}; pop(player.x,player.y-32,'1UP','#ff4d8d'); for i in range(int(0), int(20)) particles.append({x:player.x,y:player.y,vx:(randf()-.5)*8,vy:(randf()-.5)*8,life:30,c:'#ff6ec7'}); } else { sessionScore+=50000; flashMsg={t:80,txt:'MUMU SLAYER BONUS!'}; } 
