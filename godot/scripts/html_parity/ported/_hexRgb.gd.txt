## AUTO-PORTED from HTML function _hexRgb — review required
func _hexRgb(h):
	 h=(h||'#fff').replace('#',''); if(h.size()==3)h=h.split('').map(func(c): return c+c).join(''); var n=int(h,16); return [(n>>16)&255,(n>>8)&255,n&255]; 
