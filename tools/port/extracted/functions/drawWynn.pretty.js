function drawWynn(b, flash) {
  const R = b.r;
  ctx.fillStyle = flash ? '#fff' : '#5a2b3a';
  ctx.beginPath();
  ctx.ellipse(0, 10, R * 0.95, R * 0.8, 0, 0, 7);
  ctx.fill();
  ctx.fillStyle = flash ? '#fff' : '#6a3345';
  ctx.beginPath();
  ctx.arc(0, -6, R * 0.66, 0, 7);
  ctx.fill();
  ctx.fillStyle = flash ? '#fff' : '#8a4a5e';
  ctx.beginPath();
  ctx.ellipse(0, R * 0.06, R * 0.4, R * 0.3, 0, 0, 7);
  ctx.fill();
  ctx.fillStyle = '#25101a';
  circle(-R * 0.14, R * 0.02, R * 0.06, '#25101a');
  circle(R * 0.14, R * 0.02, R * 0.06, '#25101a');
  ctx.strokeStyle = '#e8dcc4';
  ctx.lineWidth = 2.5;
  ctx.beginPath();
  ctx.arc(0, R * 0.16, R * 0.14, 0.1, Math.PI - 0.1);
  ctx.stroke();
  ctx.fillStyle = flash ? '#fff' : '#e8dcc4';
  ctx.beginPath();
  ctx.moveTo(-R * 0.4, -R * 0.4);
  ctx.quadraticCurveTo(-R * 1.15, -R * 0.9, -R * 1.1, -R * 0.2);
  ctx.quadraticCurveTo(-R * 0.7, -R * 0.55, -R * 0.25, -R * 0.35);
  ctx.fill();
  ctx.beginPath();
  ctx.moveTo(R * 0.4, -R * 0.4);
  ctx.quadraticCurveTo(R * 1.15, -R * 0.9, R * 1.1, -R * 0.2);
  ctx.quadraticCurveTo(R * 0.7, -R * 0.55, R * 0.25, -R * 0.35);
  ctx.fill();
  ctx.save();
  ctx.shadowColor = '#ff2b2b';
  ctx.shadowBlur = 12;
  ctx.fillStyle = '#ff3b30';
  circle(-R * 0.22, -R * 0.14, R * 0.11, '#ff3b30');
  circle(R * 0.22, -R * 0.14, R * 0.11, '#ff3b30');
  ctx.restore();
  ctx.fillStyle = '#ffd27a';
  circle(-R * 0.22, -R * 0.16, R * 0.04, '#ffd27a');
  circle(R * 0.22, -R * 0.16, R * 0.04, '#ffd27a');
  ctx.fillStyle = '#ffd700';
  ctx.font = 'bold ' + R * 0.5 + 'px monospace';
  ctx.textAlign = 'center';
  ctx.fillText('$', 0, R * 0.7);
  ctx.textAlign = 'left';
}
